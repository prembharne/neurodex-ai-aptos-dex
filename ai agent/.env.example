# Copy this file to .env and fill in values. Do NOT commit your real .env.
#############################################
# AI Provider Keys (set at least one)
#############################################
# Groq (recommended - fast and free)
GROQ_API_KEY="REPLACE_WITH_YOUR_GROQ_API_KEY"
# Optional override (defaults to llama-3.1-70b-versatile)
GROQ_MODEL="llama-3.1-70b-versatile"

# DeepSeek (backup option)
DEEPSEEK_API_KEY="sk-a4e84c211190427da2b5c32078de00d5"
# Optional override (defaults to deepseek-chat)
DEEPSEEK_MODEL="deepseek-chat"

# OpenAI fallback
OPENAI_API_KEY="REPLACE_WITH_OPENAI_KEY"

# Anthropic (if you later enable anthropic provider manually)
ANTHROPIC_API_KEY="REPLACE_WITH_ANTHROPIC_KEY"

#############################################
# Server
#############################################
PORT=3002

#############################################
# Perpetuals / Move Deployment
#############################################
ADMIN_PRIVATE_KEY="REPLACE_WITH_ADMIN_PRIVATE_KEY_HEX" # Deployer / admin signer (temporary; move to wallet signing later)
PERP_MODULE_ADDR="0xYOUR_DEPLOYED_ACCOUNT" # Publishing account address for Move modules (no ::module suffix)

#############################################
# Notes
# - Never commit the real .env file.
# - Set only the providers you use; unset others.
# - After editing, restart:  PowerShell =>  npm run dev
#############################################
